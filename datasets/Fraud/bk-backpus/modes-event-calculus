modeh(initiatedAt(fraud(+cardId),+time))
modeh(terminatedAt(fraud(+cardId),+time))

modeb(holdsAt(increasingSequence(+cardId),+time))
%modeb(holdsAt(increasingSequence(+cardId),-time))

modeb(initiatedAt(increasingSequence(+cardId),+time))
%modeb(initiatedAt(increasingSequence(+cardId),-time))

modeb(terminatedAt(increasingSequence(+cardId),+time))
modeb(terminatedAt(increasingSequence(+cardId),-time))

modeb(holdsAt(decreasingSequence(+cardId),+time))
%modeb(holdsAt(decreasingSequence(+cardId),-time))

modeb(initiatedAt(decreasingSequence(+cardId),+time))
%modeb(initiatedAt(decreasingSequence(+cardId),-time))

modeb(terminatedAt(decreasingSequence(+cardId),+time))
modeb(terminatedAt(decreasingSequence(+cardId),-time))

modeb(holdsAt(amountTransition(+cardId,-amount,+amount),+time))
modeb(holdsAt(amountTransition(+cardId,+amount,-amount),+time))
modeb(initiatedAt(amountTransition(+cardId,-amount,+amount),+time))
modeb(initiatedAt(amountTransition(+cardId,+amount,-amount),+time))
modeb(terminatedAt(amountTransition(+cardId,-amount,+amount),+time))
modeb(terminatedAt(amountTransition(+cardId,+amount,-amount),+time))

modeb(happensAt(disappear(+cardId),+time))


examplePattern(holdsAt(fraud(+cardId),+time))
